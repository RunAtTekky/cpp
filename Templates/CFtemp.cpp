#include <iostream>
using namespace std;
 
using i64 = long long;
 
void solve() {
    // Inputs in every testcase.
    int n, k;
    i64 x;
    cin >> n >> k >> x;

    // Actual Code here
    
    
}
 
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    // Number of testcases
    int t;
    cin >> t;
    
    while (t--) {
        solve();
    }
    
    return 0;
}